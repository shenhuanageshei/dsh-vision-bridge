# 交接：运行时服务缝 v4.1（2026-09-26 交付状态）

> 本文件记录本次交付的**当前状态与未完成项**，供重启后或接手者继续。
> 权威设计：`docs/design-v4-runtime-seam.md`（v4.1）。仓库根的 `HANDOFF.md` 是 9/7 的旧快照，已被本文件取代。

---

## 0. 一句话

插件的「让纯文本模型也能收图」机制已从**改 DSH 内核源码**换成**运行时装饰 `llm.resolveModelInfo`**（详见设计 v4.1）；
代码、单测、四轮评审、文档都已完成，本机安装方式已改为 **link:（不再复制同步）**；
**唯一未做的是真机验收——需要重启 DSH 桌面版**。

---

## 1. 本次做了什么

### 代码（仓库工作区，**尚未 commit**）

| 文件 | 状态 | 说明 |
|---|---|---|
| `lib/seam.js` | 新增 | 运行时缝：装饰 + 还原 + 行为自证幂等 + 三态；导出 `resolveModelInfoUnseamed()` |
| `tests/seam.test.mjs` | 新增 | 30 用例，每条修复都有「改回旧写法必须失败」的回归 |
| `lib/index.js` | 修改 | 缝接线：`apply()` 最先装（`ctx.effect` 挂 fiber 生命周期）、整段 try/catch 兜底、`settings.watch` 驱动 卸载→重算→重装、disposer 还原 |
| `lib/config.js` | 修改 | 三个新键 `seam.mode` / `seam.include` / `seam.requireReader`（均 live；非法 `include` 整体拒绝保旧值） |
| `lib/auto.js` | 修改（2 行） | round-3 模态门禁改走未注入版本 —— **越界改动，经评审 D1 裁定采纳**（不改会让自动代读跳过，制造「发送成功但无人读图」） |
| `docs/design-v4-runtime-seam.md` | 新增 | 设计 v4.1（含 §10 逐条响应表） |
| `docs/review-design-v4.md` | 新增 | 第 1 轮设计评审（FAIL，B1–B5） |
| `docs/review-design-v4-r2.md` | 新增 | 第 2 轮设计复评（PASS，N11/N12） |
| `docs/review-impl-v4.md` | 新增 | 第 3 轮代码评审（FAIL，B-I1 + N-I1…N-I5） |
| `docs/review-impl-v4-r2.md` | 新增 | 第 4 轮修复复评（PASS，新增 N-I6 非阻断） |
| `docs/VERIFY.md` | 修改 | 新增 §12（验收清单 + 已知偏差）；§12 前置写明新的安装方式 |

### 本机安装方式变更（2026-09-26）

- `C:\Users\magic\.dsh\profiles\desktop\package.json`：
  `"@dsh-external/dsh-vision-bridge"` 由 `file:C:/Users/magic/.dsh-plugins/dsh-vision-bridge` 改为
  `link:D:/DSH-Portable/plugins/dsh-vision-bridge`。
- `pnpm install` 已在本 profile 跑过（用系统 node v24.14.1 + DSH 自带 pnpm 11.7.0，1m45s，`+10 -2`）。
- 结果：`profiles/desktop/node_modules/@dsh-external/dsh-vision-bridge` 现为 **symlink → 本仓库**；改源码不再需要 robocopy 同步。

---

## 2. 当前状态（尚未生效）

- **DSH 内核未重启**：当前内核 PID 启动于 2026-09-25 23:03:23，加载的是**旧**复制副本代码。
  新代码只在**下次启动**时被加载。
- profile 解析该包名已验证指向 `D:\DSH-Portable\plugins\dsh-vision-bridge\lib\index.js`；入口 ESM 载入成功。
- 33 个 `dsh.profile.bundles` 声明的包全部在位；`@dsh-external` 下 6 个插件完好。

---

## 3. 验证到什么程度

**已验证（本机实跑）**：`node --test tests/seam.test.mjs` = 30/30；`node --test` = **405/405**（基线 399 + 新增 6）。

**由评审独立实测（不是我复述）**：装饰落在真实实例上且可还原；`already` 行为自证；
`ctx.effect` 拆除能还原；`apply()` 中途抛错后残留被清干净；`preventExtensions`/`freeze` 降级为 unsupported；
peer 依赖全部可解析；cordis `Disposable` 契约。

**未做（需要重启后人工验收）**：`docs/VERIFY.md` §12 的 1–9 项（真机贴图端到端、多模态回归、子代理、卸载还原、live 配置、代读前置）。

---

## 4. 未做项（下一批）

| 编号 | 内容 | 位置 |
|---|---|---|
| §4.7-1 | 缝 live 时跳过磁盘自愈 `selfHealAdmissionGate` | `lib/index.js` apply ⓪ |
| §4.7-2 | 缝 live 时跳过行为探针（并处理 scratch 残留 / runtime 恒 dead） | `lib/server-routes.js` |
| §4.6 | 设置卡片四行（缝状态 / 标记残留 / 代读可用性 / 磁盘补丁遗留） | `lib/client.js` |
| N-I2 | 凭证解析加短 TTL、warn 去重（`buildModelCatalog` 会批量调用） | `lib/index.js` `readerAvailable` |
| N-I4 | `llm` 后到时重试（与 `tests/dual-gen.test.mjs` 的 inject 断言冲突，需同批放开） | `lib/index.js` |
| N-I6 | 标记写入移进 try（3 行级） | `lib/seam.js` |
| 交付件 | `tests/TEST-MATRIX.md` 映射行、`package.json` 版本 `0.1.1 → 0.2.0` | 仓库根 |

---

## 5. 如何生效 / 如何回退

**生效**：退出 DeepSeek Harness → 重新打开 → 按 `docs/VERIFY.md` §12 逐条验收。

**回退（任一层）**：

1. 依赖改回复制模式：把 `profiles/desktop/package.json` 的该行恢复为 `file:` 写法（备份见下），再跑一次 install。
2. 文件级备份（本次改动前）：
   - `profiles/desktop/package.json.bak-20260925161924-pre-link`
   - `profiles/desktop/pnpm-lock.yaml.bak-20260925161924-pre-link`
3. DSH 配置快照（`undo_list` 可查）：`20260926-001344-9642`（link 前）、`20260926-001922-ccf7`（改 link: 前）。
4. 代码回退：仓库工作区改动未提交，`git checkout -- <file>` 或 `git stash` 即可。

---

## 6. 命令备忘（本机实测可用）

```powershell
# 在 profile 里跑依赖安装（不要用 DSH 自带 node.cmd：它依赖 %DSH_DESKTOP_NODE_EXECUTABLE%，在工具沙箱里为空）
cd C:\Users\magic\.dsh\profiles\desktop
node "D:\DSH-Desktop\resources\runtime\pnpm\bin\pnpm.cjs" install --prefer-offline --config.confirmModulesPurge=false

# 抽取 app.asar 内的内核源码（DSH 的 read/grep 读不了 asar 内部路径）
node C:\Users\magic\AppData\Local\Temp\dsh-asar-extract.mjs "D:\DSH-Desktop\resources\app.asar" "dsh/node_modules/@deepseek-ai/<pkg>/lib/index.js" > out.js
```

---

## 7. 评审链（四轮，全部有书面记录）

| 轮次 | 报告 | 结论 |
|---|---|---|
| 1 | `docs/review-design-v4.md` | **FAIL**（B1–B5 + N1–N10） |
| 2 | `docs/review-design-v4-r2.md` | PASS（B1–B5 核销；N11/N12 追补） |
| 3 | `docs/review-impl-v4.md` | **FAIL**（B-I1 + N-I1…N-I5；D1/D2/D3 裁决） |
| 4 | `docs/review-impl-v4-r2.md` | PASS（B-I1/N-I1/N-I3 核销；新增 N-I6 非阻断） |

两个成员（`design-reviewer` 只读评审、`seam-implementer` 唯一写者）经共享任务板 task-1…task-6 协作；写范围互不重叠，`lib/auto.js` 的越界改动经 D1 裁定追溯授权。
